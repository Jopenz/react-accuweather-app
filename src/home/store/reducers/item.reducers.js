

const initialState = {
    items: ['lala']
};

const itemReducer = function (state = initialState, action) {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default itemReducer;